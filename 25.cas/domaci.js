function radijani(y) {
    return (par * 180) / Math.PI;
  }
  console.log(radijani(Math.PI / 6));
  
  function obim(par) {
    return 2 * r * Math.PI;
  }
  console.log(radijani(2));
  
  function povrsina(x) {
    r = r / 2;
    return r ** 2 * Math.PI;
  }
  console.log(radijani(4));

